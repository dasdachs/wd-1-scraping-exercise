from typing import List
form


def price_string_to_number(price_string: str) -> int:
    # How do we get the number
    raise NotImplemented


def results_to_models(results: List[str]) -> List[object]:
    # Convert search result list to objects
    raise NotImplemented


def models_to_csv(results: List[str]) -> List[object]:
    # Convert SearchItem models to csv
    raise NotImplemented
